---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy
spec:
  template:
    spec:
      containers:
        - name: node
          image: node
          imagePullPolicy: IfNotPresent
          env:
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: cm
                  key: environment
          ports:
            - name: node
              containerPort: 3001
          livenessProbe:
            httpGet:
              path: /health_check
              port: node
            initialDelaySeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health_check
              port: node
            initialDelaySeconds: 10
            timeoutSeconds: 5
